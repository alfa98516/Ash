#include "parser.h"
#include <ctype.h>
#include <stdio.h>
#include <stdlib.h>
#define MAX_TOKENS 512

/*
 * @brief
 * @param cmd: command to tokenize.
 */
void tokenize(char* cmd) {
    int x;
    Token t;
    struct LinkedList* tokens = init();
}
